/*
    - Exiba no console do browser a 
    quantidade de caracteres que a const possui.
*/

const linguagem = "JavaScript";

linguagem.length = linguagem;
console.log(linguagem);
console.log(linguagem.length);