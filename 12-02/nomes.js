/*
    fazer com que cada nome do array fique um um "-" antes, pulando linha 
    para cada um.
*/

const nomes = ["Fernando", "Caio", "Juliana"];

for (let i = 0; i < nomes.length; i++) {
    console.log("-" + nomes[i]);
}